@using Razor.Orm
@using Razor.Orm.Test
@inherits QueryTemplate<TestDto, TestResultDto>
select name @As(e => e.Name), fullname @As(e => e.Result.Name) from users
@using (Where())
{
    if (Model.Name != null)
    {
        <text>name = @Par(Model.Name) and id @In(1, 2, 3)</text>
    }
}