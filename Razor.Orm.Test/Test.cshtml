@using Razor.Orm
@using Razor.Orm.Test
@inherits QueryTemplate<TestDto, TestResultDto>
select name @As(e => e.Name) from users
@using (Where())
{
    if (Model.Name != null)
    {
        <text>name = @Par(Model.Name) and id @InParams(1, 2, 3)</text>
    }
}